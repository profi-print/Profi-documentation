# ProfitPrint — Система документооборота

Полноценное веб-приложение (SPA) для замены Excel-файлов в типографии.  
Автоматизирует создание, учёт и печать заказов на производство, накладных, актов сверки.  
Данные синхронизируются между веб-интерфейсом, облачной MongoDB и Telegram-ботом.

---

## Содержание
1. [Функциональные возможности](#функциональные-возможности)
2. [Структура проекта](#структура-проекта)
3. [Технологический стек](#технологический-стек)
4. [Архитектура данных](#архитектура-данных)
5. [Веб-приложение (фронтенд)](#веб-приложение-фронтенд)
6. [Серверная часть (бэкенд)](#серверная-часть-бэкенд)
7. [Telegram-бот](#telegram-бот)
8. [Генерация PDF-документов](#генерация-pdf-документов)
9. [Корзина (Trash)](#корзина-trash)
10. [Инструкция по развёртыванию](#инструкция-по-развёртыванию)
11. [Часто задаваемые вопросы (FAQ)](#часто-задаваемые-вопросы-faq)
12. [Лицензия и контакты](#лицензия-и-контакты)

---

## Функциональные возможности

- **Дашборд** – статистика, финансовая сводка, последние документы, быстрый старт.
- **Клиенты** – справочник с поиском, создание/редактирование/удаление (в корзину), детализация.
- **Продукты** – номенклатура с артикулом, наименованием, ценой, количеством, стоимостью (авторасчёт), произведено, заказано.
- **Заказы на производство** – создание, редактирование, удаление; автоматическая подстановка данных из продукта; таблица позиций с итогами.
- **Накладные** – создание, в том числе копированием из заказа; автоподстановка количества из продукта; итог скрыт при <2 позициях.
- **Акты сверки** – полностью ручной ввод (как в Excel); добавление строк, автовычисление конечного остатка, итоговая строка с суммами по столбцам.
- **Резервные копии (корзина)** – все удалённые документы сохраняются; просмотр полной информации, восстановление, безвозвратное удаление, экспорт.
- **PDF** – генерация фирменных документов (красный логотип, синие заголовки, числа без "UZS", прочерки вместо нулей).
- **Telegram-бот** – интерактивное меню, получение PDF и детализации документов напрямую из облачной базы.
- **Синхронизация** – между веб-приложением, сервером Node.js и MongoDB Atlas; кнопка «Синхронизировать» на дашборде.
- **Экспорт/Импорт данных** в JSON.

---

## Структура проекта
profitprint/
├── index.html # Дашборд
├── orders.html # Заказы на производство
├── invoices.html # Накладные
├── clients.html # Клиенты
├── products.html # Продукты (номенклатура)
├── reconciliations.html # Акты сверки
├── backups.html # Резервные копии (корзина)
├── .env # Переменные окружения (Telegram-бот)
├── bot.py # Telegram-бот
├── css/
│ ├── theme.css # Цветовая схема
│ ├── style.css # Основные стили
│ └── documents.css # Стили для таблиц и PDF
├── js/
│ ├── utils.js # Вспомогательные функции (даты, маска, формат)
│ ├── storage.js # LocalStorage + синхронизация с API
│ ├── pdf-generator.js # Генерация PDF в браузере
│ ├── main.js # Логика дашборда
│ ├── orders.js # Логика заказов
│ ├── invoices.js # Логика накладных
│ ├── reconciliations.js # Логика актов сверки
│ ├── backups.js # Логика корзины
│ └── telegram-sender.js # (устарел) Отправка JSON боту
├── server/ # Серверная часть (Node.js/Express)
│ ├── package.json
│ ├── .env # MONGODB_URI для сервера
│ ├── server.js
│ └── models/
│ ├── Client.js
│ ├── Product.js
│ ├── Order.js
│ ├── Invoice.js
│ ├── Payment.js
│ └── Reconciliation.js
└── assets/
└── logo/ # Папка для логотипа (опционально)

text

---

## Технологический стек

| Компонент       | Технологии                          |
|-----------------|-------------------------------------|
| Фронтенд        | HTML5, CSS3, JavaScript (без фреймворков) |
| PDF в браузере  | html2pdf.js                         |
| Бэкенд          | Node.js, Express.js, Mongoose       |
| База данных     | MongoDB Atlas (или локальный MongoDB)|
| Telegram-бот    | Python 3, python-telegram-bot, pdfkit, pymongo, python-dotenv |
| Генератор PDF   | wkhtmltopdf (через pdfkit)          |
| Инфраструктура  | REST API, LocalStorage, JSON        |

---

## Архитектура данных

Данные хранятся в трёх слоях:
1. **LocalStorage** браузера (быстрый доступ, офлайн-режим).
2. **MongoDB** (центральное облачное хранилище).
3. **JSON-файлы** (экспорт/импорт, резервные копии).

### Основные коллекции
- **clients** – id, name, contact, phone, address
- **products** – id, sku, name, price, unit, quantity, cost, produced, ordered
- **orders** – id, number, date, completionDate, clientId, items[]
- **invoices** – id, number, date, clientId, items[]
- **payments** – id, clientId, date, amount, description
- **reconciliations** – id, number, date, clientId, periodFrom, periodTo, items[]
- **trash** – массив удалённых объектов с типом, самим объектом и датой удаления

---

## Веб-приложение (фронтенд)

### Дашборд
- Карточки-счётчики (заказы, накладные, акты, клиенты, продукты)
- Финансовая сводка (реализация, оплаты, задолженность)
- Последние документы с кнопками: **📄 PDF**, **🗑️ Удалить**, **📋 Детали**
- Кнопка **🔄 Синхронизировать** для загрузки данных с сервера
- Быстрый старт (5 шагов) и справка

### Клиенты и Продукты
- Таблицы с поиском, создание/редактирование/удаление (с перемещением в корзину)
- При клике на строку — детальная информация в модальном окне

### Заказы
- Форма с выбором клиента, датами, таблицей позиций
- При выборе продукта автоматически заполняются: артикул, произведено, заказано, цена
- Стоимость пересчитывается автоматически

### Накладные
- Возможность создания из заказа (копирование позиций)
- Количество автоматически подставляется из карточки продукта
- Итого скрывается, если позиций меньше двух

### Акты сверки
- **Полностью ручной ввод** (как в Excel)
- Кнопка **+ Добавить строку** создаёт редактируемые поля
- **Конечный остаток** = Начальный остаток + Реализация − Оплата (автоматически)
- **Итоговая строка** (голубой фон) суммирует все столбцы

### Корзина (Резервные копии)
- Просмотр всех удалённых элементов
- Детализация (полный JSON), восстановление, полное удаление
- Экспорт отдельного элемента или всей корзины

---

## Серверная часть (бэкенд)

Сервер на Node.js/Express обеспечивает REST API для синхронизации данных с фронтендом и Telegram-ботом.

### API-эндпоинты
| Метод   | Путь                     | Описание                  |
|---------|--------------------------|---------------------------|
| GET     | /api/data                | Получить все данные       |
| POST    | /api/data                | Импортировать все данные  |
| GET     | /api/:collection         | Получить коллекцию        |
| POST    | /api/:collection         | Создать запись            |
| PUT     | /api/:collection/:id     | Обновить запись           |
| DELETE  | /api/:collection/:id     | Удалить запись            |

Коллекции: `clients`, `products`, `orders`, `invoices`, `payments`, `reconciliations`

### Запуск сервера
```powershell
cd server
npm install
# Настроить MONGODB_URI в server/.env
npm start
Telegram-бот
Бот работает на Python, напрямую подключаясь к MongoDB.

Команды
/start – главное меню с инлайн-клавиатурой

📋 Заказы / 📦 Накладные / 📑 Акты сверки – выбор типа документа

Внутри каждого раздела – список номеров, при выборе:

📄 Скачать PDF

📋 Подробнее (таблица позиций)

↩️ Назад

Особенности
Все PDF генерируются из актуальных данных MongoDB

Форматирование: красный логотип, синие заголовки, числа без "UZS", прочерки

Не требует ручной синхронизации – данные обновляются при сохранении через веб-приложение

Запуск бота
powershell
# Установка зависимостей (Python 3)
pip install python-telegram-bot pdfkit pymongo python-dotenv
# Установить wkhtmltopdf (https://wkhtmltopdf.org/downloads.html)
# Настроить .env (BOT_TOKEN, MONGODB_URI) и путь к wkhtmltopdf в bot.py
python bot.py
Генерация PDF-документов
PDF формируются двумя способами:

В браузере (кнопка «📄 PDF» в карточке или дашборде) – библиотека html2pdf.js.

В Telegram-боте – библиотека pdfkit, использующая wkhtmltopdf.

Оба способа используют одинаковые HTML-шаблоны, обеспечивая идентичный вид.

Структура PDF:

Шапка: красный логотип PROFI PRINT, реквизиты, название документа

Таблица с синими заголовками

Данные: числа с пробелами-разделителями, пустые значения – прочерк

Итоговая строка (одна для акта сверки, стандартная для заказов/накладных)

Корзина (Trash)
При удалении любого документа он не исчезает навсегда, а перемещается в корзину.
В корзине можно:

Просмотреть полную информацию (все поля, JSON)

Восстановить документ

Удалить безвозвратно

Экспортировать отдельный элемент или всю корзину в JSON

Корзина хранится в localStorage (pp_trash).

Инструкция по развёртыванию
1. Подготовка окружения
Установите Node.js (LTS) и Python 3.9+

Для Python установите зависимости: pip install python-telegram-bot pdfkit pymongo python-dotenv

Скачайте и установите wkhtmltopdf для Windows

2. Настройка MongoDB Atlas (или локальной базы)
Создайте кластер на MongoDB Atlas

В разделе Database Access создайте пользователя с правами Atlas admin

В Network Access добавьте IP 0.0.0.0/0 (или ваш текущий IP)

Скопируйте строку подключения (URI)

3. Конфигурация сервера
В папке server создайте файл .env:

text
MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.xxxx.mongodb.net/profitprint?retryWrites=true&w=majority
PORT=3000
Выполните в терминале:

powershell
cd server
npm install
npm start
4. Конфигурация Telegram-бота
Получите токен у @BotFather

В корне проекта создайте файл .env:

text
BOT_TOKEN=ваш_токен
MONGODB_URI=такая_же_как_на_сервере
В файле bot.py укажите правильный путь к wkhtmltopdf.exe (по умолчанию C:\Program Files\wkhtmltopdf\bin\wkhtmltopdf.exe)

Запустите бота: python bot.py

5. Запуск веб-приложения
Откройте index.html в браузере (можно через Live Server)

Нажмите кнопку 🔄 Синхронизировать для загрузки данных с сервера

Создавайте клиентов, продукты, заказы, накладные и акты сверки

Часто задаваемые вопросы (FAQ)
В. Почему не сохраняются данные после перезагрузки?
О. Нажмите "Синхронизировать" на дашборде – это загрузит данные с сервера в localStorage. Изменения автоматически уходят на сервер при каждом сохранении.

В. Бот не отвечает или пишет "Документ не найден".
О. Убедитесь, что сервер запущен, данные синхронизированы, и в MongoDB есть документы. Проверьте, что BOT_TOKEN и MONGODB_URI в .env корректны.

В. PDF содержит кракозябры вместо русского текста.
О. Убедитесь, что во всех HTML-шаблонах присутствует <meta charset="UTF-8">. В актуальной версии это уже исправлено.

В. Можно ли работать без сервера и MongoDB?
О. Да, веб-приложение может работать только с localStorage. Однако Telegram-бот и синхронизация между устройствами будут недоступны.

В. Как перенести данные на другой компьютер?
О. Используйте кнопки 📤 Экспорт и 📥 Импорт на дашборде.

Лицензия и контакты
Проект разработан для типографии ProfitPrint.
При возникновении вопросов или предложений обращайтесь к сопровождающему ИИ-помощнику.

© 2026 ProfitPrint. Все права защищены.